/* INTEGRAÇÃO: upsell-3 — Manutenção 40+
 * Reservado para o script oficial que será enviado posteriormente.
 * Nenhuma cobrança está habilitada.
 *
 * Opção A: inserir o widget oficial em #platform-upsell e executar:
 * window.CarnivoraUpsell.usePlatformButton();
 *
 * Opção B: conectar o botão padrão ao SDK oficial:
 * window.CarnivoraUpsell.registerAccept(async function (context) {
 *   // Chamar a integração oficial e aguardar confirmação verificável.
 *   // Apenas após APROVAÇÃO, navegar para context.nextUrl.
 *   // Erro/cancelamento NÃO deve avançar como compra aprovada.
 * });
 *
 * Não cole tags <script> neste .js; tags vão no HTML correspondente.
 * Veja INTEGRACAO.md. Nunca inclua chaves secretas no frontend.
 */

